eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/
  instance:
    hostname: localhost
    

server:
  port: 1111
